apiVersion: apps/v1
kind: Deployment
metadata:
  name: youves-__PROJECT_NAME__
  namespace: youves-__NAMESPACE_NAME__
  labels:
    app: youves-__PROJECT_NAME__
spec:
  selector:
    matchLabels:
      app: youves-__PROJECT_NAME__
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: youves-__PROJECT_NAME__
    spec:
      containers:
        - name: youves-__PROJECT_NAME__
          image: __TO_BE_REPLACED_BY_IMAGE_TAG__
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: youves-__PROJECT_NAME__-config-map
            # - secretRef:
            #     name: youves-__PROJECT_NAME__-secret
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 60
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 60
            timeoutSeconds: 5
      imagePullSecrets:
        - name: regsecret
      restartPolicy: Always
status: {}
