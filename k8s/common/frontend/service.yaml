apiVersion: v1
kind: Service
metadata:
  name: __PROJECT_NAME__-service
  namespace: __NAMESPACE_NAME__
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
  selector:
    app: __PROJECT_NAME__
