apiVersion: v1
kind: Service
metadata:
  name: cat-test-__CI_PROJECT_NAME__-service
  namespace: cat-__NAMESPACE_NAME__
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
  selector:
    app: cat-test-__CI_PROJECT_NAME__
